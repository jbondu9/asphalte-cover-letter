{
  "name": "my-app",
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "start": "nuxt start",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "nuxt": "^2.14.12"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "push-dir": "^0.4.1"
  }
}
